Before writing any code for: $ARGUMENTS

1. Use `/serpentine` to identify relevant files and dependencies — run `serpentine catalog` and `serpentine analyze` before reading any files manually.
2. Ask me clarifying questions about requirements, edge cases, and constraints
3. Write a concise implementation plan to a scratch file IN THE CURRENT DIRECTORY at .claude/scratch/spec-[feature].md. For large features, break it down into smaller steps.
4. Wait for my approval before writing any code
